{
  "name": "calculadora-tope-reintegro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "24.18.0",
    "npm": "11.16.0"
  },
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "vite build && node scripts/write-version.js",
    "preview": "vite preview",
    "test:browser": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
